# Hearth Pi 5 boot config
kernel=Image

# Enable DRM/KMS GPU driver (required for flutter-pi)
dtoverlay=vc4-kms-v3d

# GPU memory for rendering
gpu_mem=256

# Disable overscan
disable_overscan=1

# Faster boot
disable_splash=1
boot_delay=0

# HDMI safe defaults (flutter-pi handles resolution)
hdmi_force_hotplug=1
